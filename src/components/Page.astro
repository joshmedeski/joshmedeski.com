---
import Layout from './Layout.astro'
import clsx from 'clsx'

const {
  content: { title, titleClasses, description, hideCta, image },
} = Astro.props
---

<Layout title={title} description={description} hideCta={hideCta} image={image}>
  <main class="pb-20 pt-10 bg-neutral-950">
    <article
      class="prose prose-invert mx-auto max-w-screen-md bg-black px-8 prose-a:text-blue-600 hover:prose-a:text-blue-500 pb-10"
    >
      <h1
        class={clsx([
          "mb-2 text-center text-2xl font-extrabold md:text-4xl",
          titleClasses,
        ])}
      >
        {title}
      </h1>
      <slot />
    </article>
  </main>
</Layout>
